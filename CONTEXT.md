# 盒记领域词汇

盒记记录盒饭档口的接单、配送、次卡扣次和对账闭环。本文件固定业务语言，避免“月卡”“次卡”和模板类型在后续实现中混用。

## 订单与次卡

**次卡**：按次数消费、无有效期的客户套餐记录；配送完成时从客户的次卡余额池扣除本单承诺使用的次数。
_Avoid_: 储值卡、余额卡

**月卡**：次卡面向客户的对外称呼，不代表系统存在月份或有效期字段。
_Avoid_: 有效期卡、月度订阅

**本次使用份数**：一张已配送订单实际从次卡扣除的份数；组合支付时只计算次卡部分，不计算微信或现金部分。

**当前可用份数**：配送完成后，客户所有 active 次卡的实际剩余次数减去其他待配送订单的次卡预占次数。

## 文案

**菜单文案模板**：用于把日期和午餐 / 晚餐内容渲染成社群菜单文案的模板。

**月卡文案模板**：用于把套餐说明和次卡使用信息渲染成可复制给客户的文案模板。套餐描述直接写在正文中并可随意修改，系统只替换本次使用份数和当前可用份数。
_Avoid_: 通用模板、菜单模板

**默认模板**：某一类文案复制操作当前使用的模板；菜单模板和月卡模板各自维护自己的默认模板。
