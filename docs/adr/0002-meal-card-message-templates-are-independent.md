# 月卡文案模板使用独立数据表

月卡信息复制使用独立的模板表、版本表和维护页面，与菜单文案模板分开；两类模板虽然共享默认、历史、恢复和删除交互，但保留各自的占位符校验与默认状态。这样可以让菜单模板继续使用餐次区块语法，也能让月卡模板只校验“本次使用份数”和“当前可用份数”，避免两种业务的默认约束、备份结构和正文语义互相污染。月卡文案中的当前剩余份数取配送后 active 次卡的实际剩余次数，不扣除待配送订单的预占；预占扣除只用于订单可用性校验。
